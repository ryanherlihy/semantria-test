var leveljs = require('level-js')
var levelup = require('levelup')
var filesystem = require('level-filesystem')

var db = levelup('browser-fs', { db: leveljs })
var fs = filesystem(db)

module.exports = fs